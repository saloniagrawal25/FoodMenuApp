import {StyleSheet, TouchableOpacity} from 'react-native';

const MealsList = ({icon, color, onPress}) => {
  return <View></View>;
};

const styles = StyleSheet.create({});

export default MealsList;
